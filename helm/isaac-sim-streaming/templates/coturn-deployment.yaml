# This project was developed with assistance from AI tools.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: coturn
  namespace: {{ include "isaac-sim-streaming.namespace" . }}
  labels:
    app.kubernetes.io/name: coturn
    {{- include "isaac-sim-streaming.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: coturn
  template:
    metadata:
      labels:
        app.kubernetes.io/name: coturn
    spec:
      serviceAccountName: coturn
      automountServiceAccountToken: false
      containers:
        - name: coturn
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          image: {{ .Values.coturn.image.repository }}:{{ .Values.coturn.image.tag }}
          command:
            - /bin/sh
            - -c
            - |
              set -e
              TURN_PASSWORD=$(cat /etc/coturn/auth/password)
              sed "s|TURN_PASSWORD_PLACEHOLDER|${TURN_PASSWORD}|g" \
                /etc/coturn/config/turnserver.conf > /tmp/turnserver.conf
              exec turnserver -c /tmp/turnserver.conf
          ports:
            - name: turn
              containerPort: 3478
              protocol: TCP
            - name: turns
              containerPort: 5349
              protocol: TCP
          volumeMounts:
            - name: config
              mountPath: /etc/coturn/config
              readOnly: true
            - name: tls
              mountPath: /etc/coturn/tls
              readOnly: true
            - name: auth
              mountPath: /etc/coturn/auth
              readOnly: true
            - name: tmp
              mountPath: /tmp
          resources:
            requests:
              cpu: {{ .Values.coturn.resources.requests.cpu }}
              memory: {{ .Values.coturn.resources.requests.memory }}
            limits:
              cpu: {{ .Values.coturn.resources.limits.cpu | quote }}
              memory: {{ .Values.coturn.resources.limits.memory }}
      volumes:
        - name: config
          configMap:
            name: coturn-config
        - name: tls
          secret:
            secretName: coturn-tls
        - name: auth
          secret:
            secretName: coturn-auth
        - name: tmp
          emptyDir: {}
