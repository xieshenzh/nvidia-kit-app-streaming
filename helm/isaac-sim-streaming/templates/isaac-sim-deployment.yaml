# This project was developed with assistance from AI tools.
apiVersion: apps/v1
kind: Deployment
metadata:
  name: isaac-sim
  namespace: {{ include "isaac-sim-streaming.namespace" . }}
  labels:
    app.kubernetes.io/name: isaac-sim
    {{- include "isaac-sim-streaming.labels" . | nindent 4 }}
spec:
  replicas: 1
  strategy:
    type: Recreate
  selector:
    matchLabels:
      app.kubernetes.io/name: isaac-sim
  template:
    metadata:
      labels:
        app.kubernetes.io/name: isaac-sim
    spec:
      serviceAccountName: isaac-sim
      automountServiceAccountToken: false
      {{- if or .Values.ngc.apiKey .Values.isaacSim.image.pullSecret }}
      imagePullSecrets:
        {{- if .Values.ngc.apiKey }}
        - name: ngc-pull-secret
        {{- end }}
        {{- if .Values.isaacSim.image.pullSecret }}
        - name: {{ .Values.isaacSim.image.pullSecret }}
        {{- end }}
      {{- end }}
      nodeSelector:
        nvidia.com/gpu.product: {{ .Values.isaacSim.gpu.product }}
      runtimeClassName: {{ .Values.isaacSim.gpu.runtimeClassName }}
      tolerations:
        - key: nvidia.com/gpu
          operator: Exists
          effect: NoSchedule
      securityContext:
        fsGroup: 0
      containers:
        - name: isaac-sim
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            capabilities:
              drop:
                - ALL
          {{- if eq .Values.app "isaac-lab" }}
          image: {{ .Values.isaacLab.image.repository }}:{{ .Values.isaacLab.image.tag }}
          imagePullPolicy: {{ .Values.isaacLab.image.pullPolicy }}
          command:
            - /bin/bash
            - -c
            - |
              set -e
              /isaac-sim/runheadless.sh \
                --/app/livestream/webrtcEnabled=true \
                --exec /scripts/{{ .Values.isaacLab.script }}
          {{- else }}
          image: {{ .Values.isaacSim.image.repository }}:{{ .Values.isaacSim.image.tag }}
          imagePullPolicy: {{ .Values.isaacSim.image.pullPolicy }}
          command:
            - /bin/bash
            - -c
            - |
              set -e
              /isaac-sim/runheadless.sh \
                --/app/livestream/webrtcEnabled=true
          {{- end }}
          env:
            - name: ACCEPT_EULA
              value: "Y"
            - name: PRIVACY_CONSENT
              value: "Y"
            - name: OMNI_KIT_ACCEPT_EULA
              value: "Y"
            - name: NVIDIA_VISIBLE_DEVICES
              value: "all"
            - name: NVIDIA_DRIVER_CAPABILITIES
              value: "all"
            - name: WARP_CACHE_PATH
              value: /tmp/warp_cache
            {{- if eq .Values.app "isaac-lab" }}
            - name: LIVESTREAM
              value: "2"
            {{- end }}
          ports:
            - name: signaling
              containerPort: 49100
              protocol: TCP
          resources:
            requests:
              cpu: {{ .Values.isaacSim.resources.requests.cpu | quote }}
              memory: {{ .Values.isaacSim.resources.requests.memory }}
              nvidia.com/gpu: 1
            limits:
              cpu: {{ .Values.isaacSim.resources.limits.cpu | quote }}
              memory: {{ .Values.isaacSim.resources.limits.memory }}
              nvidia.com/gpu: 1
          volumeMounts:
            {{- if eq .Values.app "isaac-lab" }}
            - name: scripts
              mountPath: /scripts
              readOnly: true
            {{- end }}
            - name: cache
              mountPath: /isaac-sim/.cache/ov
              subPath: cache
            - name: cache
              mountPath: /isaac-sim/.nv/ComputeCache
              subPath: compute-cache
            - name: cache
              mountPath: /isaac-sim/.nvidia-omniverse
              subPath: nvidia-omniverse
            - name: tmp
              mountPath: /tmp
            - name: cache-local
              mountPath: /isaac-sim/.local
            - name: kit-data
              mountPath: /isaac-sim/kit/data
            - name: kit-logs
              mountPath: /isaac-sim/kit/logs
          readinessProbe:
            tcpSocket:
              port: signaling
            initialDelaySeconds: 120
            periodSeconds: 15
            timeoutSeconds: 5
            failureThreshold: 40
          livenessProbe:
            tcpSocket:
              port: signaling
            initialDelaySeconds: 600
            periodSeconds: 30
            timeoutSeconds: 10
            failureThreshold: 6
        - name: tls-proxy
          securityContext:
            runAsNonRoot: true
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: true
            capabilities:
              drop:
                - ALL
          image: quay.io/xiezhang7/isaac-sim-tls-proxy:v1
          ports:
            - name: signaling-tls
              containerPort: 8443
              protocol: TCP
          volumeMounts:
            - name: tls-proxy-conf
              mountPath: /etc/nginx/nginx.conf
              subPath: nginx.conf
              readOnly: true
            - name: tls
              mountPath: /etc/nginx/tls
              readOnly: true
            - name: nginx-tmp
              mountPath: /tmp
            - name: nginx-cache
              mountPath: /var/cache/nginx
            - name: nginx-run
              mountPath: /var/run
            - name: nginx-log
              mountPath: /var/log/nginx
          readinessProbe:
            tcpSocket:
              port: signaling-tls
            initialDelaySeconds: 5
            periodSeconds: 10
          resources:
            requests:
              cpu: 50m
              memory: 64Mi
            limits:
              cpu: 200m
              memory: 128Mi
      volumes:
        {{- if eq .Values.app "isaac-lab" }}
        - name: scripts
          configMap:
            name: isaac-lab-scripts
            defaultMode: 0444
        {{- end }}
        - name: cache
          persistentVolumeClaim:
            claimName: isaac-sim-cache
        - name: tmp
          emptyDir:
            sizeLimit: 10Gi
        - name: cache-local
          emptyDir: {}
        - name: kit-data
          emptyDir: {}
        - name: kit-logs
          emptyDir: {}
        - name: tls
          secret:
            secretName: coturn-tls
        - name: tls-proxy-conf
          configMap:
            name: tls-proxy-config
        - name: nginx-tmp
          emptyDir: {}
        - name: nginx-cache
          emptyDir: {}
        - name: nginx-run
          emptyDir: {}
        - name: nginx-log
          emptyDir: {}
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: isaac-sim-cache
  namespace: {{ include "isaac-sim-streaming.namespace" . }}
  labels:
    app.kubernetes.io/name: isaac-sim
    {{- include "isaac-sim-streaming.labels" . | nindent 4 }}
spec:
  accessModes:
    - ReadWriteOnce
  {{- if .Values.isaacSim.cache.storageClass }}
  storageClassName: {{ .Values.isaacSim.cache.storageClass }}
  {{- end }}
  resources:
    requests:
      storage: {{ .Values.isaacSim.cache.size }}
